import SwiftUI

struct Theme {
    static let primaryColor = Color.blue
    static let secondaryColor = Color.gray
    static let primaryFont = Font.system(size: 18, weight: .medium)
    static let secondaryFont = Font.system(size: 14, weight: .light)
}
